{
    "name": "synkrasis-platform",
    "version": "1.0.0",
    "scripts": {
        "dev": "cd infrastructure && docker-compose up",
        "dev:build": "cd infrastructure && docker-compose up --build",
        "dev:down": "cd infrastructure && docker-compose down",
        "frontend": "cd frontend && npm run dev",
        "backend": "cd backend && uvicorn app.main:app --reload"
    }
}